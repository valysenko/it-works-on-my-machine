apiVersion: v1
kind: Service
metadata:
  name: {{.Release.Name}}-frontend-service
spec:
  selector: # which pods are represented by this service
    app: {{.Release.Name}}-frontend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30090
  type: NodePort